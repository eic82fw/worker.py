[project]
name = "python-github-worker"
version = "0.1.0"
description = "Cloudflare Python Worker"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]
